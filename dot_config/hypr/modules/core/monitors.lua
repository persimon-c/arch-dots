
-- modules/core/monitors.lua
-- Monitor configuration — single monitor setup.
 
hl.monitor({
    output = "",           -- empty string = all monitors / auto-detect
    mode = "1920x1080@60",
    position = "0x0",
    scale = 1,
})
 